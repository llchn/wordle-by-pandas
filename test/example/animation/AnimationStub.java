package example.animation;

/**
 * Instead of drawing a window to display the animation, this
 * class only defines the methods that we need to have to 
 * check that the animation is moving correctly.
 */
public class AnimationStub extends AnimationDemo {

    public int getWidth() {
        return 100;
    }

}
